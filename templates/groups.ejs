<div class="table-container">
  <table class="table is-bordered">
    <thead>
      <tr>
        <th>id</th>
        <th>user</th>
        <th>role</th>
      </tr>
    </thead>
    <tbody>
      <% for (let i = 0; i < rows.length; i++) {%>
        <tr>
          <td><%= rows[i].id %></td>
          <td><%= rows[i].user_id %></td>
          <td><%= rows[i].role_id %></td>
        </tr>
      <% } %>
    </tbody>
  </table>
</div>
